// print text to console
console.log("Hello World!");

// the contents and datatype of a let variable can be changed
let message = "Tjoho!";
let number = 100;
number = false;

// the contents and datatype of a const variable can NOT be changed
const name = "Anders";
// name = 1000; // This will not work!

console.log(`Message: ${message} Name: ${name} Number: ${number}`);
